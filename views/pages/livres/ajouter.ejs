<%- include('../../partials/header') %>
<%- include('../../partials/navigation') %>

<main class="container">
  <h1>Ajouter un livre</h1>

  <form action="/livres/ajouter" method="POST" class="form-card">
    <label>Titre</label>
    <input type="text" name="titre" required>

    <label>Auteur</label>
    <select name="auteur_id" required>
      <% auteurs.forEach(auteur => { %>
        <option value="<%= auteur.id %>"><%= auteur.prenom %> <%= auteur.nom %></option>
      <% }) %>
    </select>

    <label>Année de publication</label>
    <input type="number" name="annee_publication">

    <label>Genre</label>
    <input type="text" name="genre">

    <label>ISBN</label>
    <input type="text" name="isbn">

    <label>Résumé</label>
    <textarea name="resume" rows="5"></textarea>

    <label class="checkbox">
      <input type="checkbox" name="disponible" checked>
      Disponible
    </label>

    <button type="submit" class="btn">Ajouter</button>
    <a href="/livres" class="btn secondary">Annuler</a>
  </form>
</main>

<%- include('../../partials/footer') %>